<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Dashboard - Homepage</title>
  <link rel="stylesheet" href="styles/styles.css"/>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      margin: 0;
      font-family: Arial, sans-serif;
      background-color: #eaeaea;
      display: flex;
      flex-direction: column;
      min-height: 100vh;
    }
    .navbar {
      background-color: #343a40;
    }
    .navbar-brand, .navbar-nav .nav-link {
      color: #ffffff !important;
    }
    .quote-details {
      background: #fff;
      padding: 25px;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0,0,0,0.1);
      width: 70%;
      margin: 30px auto 20px;
    }
    .button-container {
      display: flex;
      justify-content: center;
      gap: 15px;
      flex-wrap: wrap;
      margin-bottom: 40px;
    }
    .button {
      text-decoration: none;
      padding: 10px 20px;
      border-radius: 5px;
      color: white;
      background: #007bff;
      border: none;
      cursor: pointer;
    }
    .button:hover {
      background: #0056b3;
    }
    .btn-success {
      background: #28a745;
      color: white;
    }
    .btn-success:hover {
      background: #218838;
    }
    footer {
      background-color: #343a40;
      color: #ffffff;
      text-align: center;
      padding: 12px 0;
      margin-top: auto;
    }
  </style>
</head>
<body>

  <nav class="navbar navbar-expand-lg">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">Dashboard</a>
      <div class="collapse navbar-collapse">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <a class="nav-link" href="/overview?reference=<%= data.reference %>">Overview</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/allitems">Items</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/Billing">Billing</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/print?reference=<%= data.reference %>">Print</a>
          </li>
          <li class="nav-item">
            <a class="nav-link text-danger" href="/login">Logout</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <div class="quote-details">
    <h2 class="mb-4">Quote Details</h2>
    <p><strong>Job Description:</strong> <%= data.job_description %></p>
    <p><strong>Sales Person:</strong> <%= data.sale_person %></p>
    <p><strong>Sales Cell:</strong> <%= data.sale_cell %></p>
    <p><strong>Sales Email:</strong> <%= data.sale_email %></p>
    <p><strong>Customer Name:</strong> <%= data.customer_name %></p>
    <p><strong>Customer Cell:</strong> <%= data.customer_cell %></p>
    <p><strong>Customer Email:</strong> <%= data.customer_email %></p>
  </div>

  <div class="button-container">
    <a href="/additems?reference=<%= data.reference %>" class="button">Add Items</a>
    <a href="/Billing" class="btn btn-success">Go to Billing</a>
    <a href="/overview?reference=<%= data.reference %>" class="button">Overview</a>
    <a href="/print?reference=<%= data.reference %>" class="button">Print</a>
    <a href="/allitems" class="btn btn-success">View Items</a>
  </div>

  <footer>
    <p>&copy; <%= new Date().getFullYear() %> Bidvest Protea Coin</p>
  </footer>

</body>
</html>
